// This is free and unencumbered software released into the public domain.

const cr = @import("../abi/cr.zig");

pub export fn crRegisterErrorCallback(context: cr.context, callback: cr.error_callback_f) void {
    // TODO
}
